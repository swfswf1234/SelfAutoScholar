# 工作日志 2026-05-17

## 完成内容

### 官方文档镜像 — wget --mirror 优化与扩展

#### 代码变更

| 操作 | 文件 | 说明 |
|------|------|------|
| 修改 | `app/tools/wget_mirror.py` | `mirror()` 增加 `wait` 参数，支持每源不同速度 |
| 修改 | `app/collectors/doc_scraper.py` | DOC_SOURCES 改为 `(url, wait)` 元组格式 |
| 修改 | `scripts/hunt_docs.py` | 兼容 DOC_SOURCES 新格式 |
| 新建 | `app/tools/serve_docs.py` | 独立 HTTP 服务，解决 file:// 协议下排版问题 |
| 修改 | `app/tools/__init__.py` | 注册 serve_docs |

#### PyTorch 文档镜像 (1549 files, 322MB)

- URL 修正：从 `pytorch.org/docs/stable/`（JS 重定向页）改为 `docs.pytorch.org/docs/2.12/`
- wait=0.5s 提速，避免 --wait=2 导致 3+ 小时
- 文档结构：`docs/2.12/` 下包含全部 API 参考、开发者笔记、社区文档

#### YOLO 文档镜像 (1751 files, 534MB, partial)

- wait=1s，但 2 小时后超时
- `docs.ultralytics.com` 使用 Next.js，wget 可能陷入无限递归
- 已下载 1751 文件足够浏览，完整镜像需改用其他方案

#### DocServer 验证结果

| 验证项 | 结果 |
|--------|------|
| 入口页 `http://127.0.0.1:8080/` | ✅ 200，列出 4 个项目卡片 |
| Python 资源加载 (CSS/JS/IMG) | ✅ 全部正常，通过 `translate_path` 处理 wget 文件名 |
| PyTorch 文档页面 | ✅ 排版正常，CSS 208KB, JS 1.4MB 等均加载 |

**关键修复**：wget `--adjust-extension` 将 `theme.css?digest=xxx` 保存为 `theme.css?digest=xxx.css`，DocServer 的 `translate_path` 重写后通过 glob 匹配正确文件。

### LibGen 重试 — T-201

- 通过代理 `http://127.0.0.1:7897` 搜索已可达
- 但下载服务器（randombook.org）连接失败，仍无法下载
- 现有 17 本 PDF 保持不变
- 结论：需引入替代源（见 `docs/discuss/book_hunter_alternative_sources.md`）

## 已知遗留问题

1. YOLO 镜像超时：Next.js 站点 wget 可能无限递归
2. CDN 外部资源：PyTorch Sphinx 主题引用 Google Fonts，需联网
3. PyTorch 版本 URL 硬编码为 2.12，版本更新需手动改

## 更新文档

| 文件 | 变更 |
|------|------|
| `docs/trackers/todos.md` | T-203 进度更新为全部完成（YOLO 标记 partial），新增遗留问题 |
| `docs/knowledge_base/inventory.md` | LibGen 状态更新为"搜索可达，下载失败" |
| `docs/discuss/book_hunter_alternative_sources.md` | 新建，讨论替代源方案 |
| `docs/design/doc_viewer_guide.md` | 新建，DocServer 使用手册 |
| `docs/design/pytorch_docs_guide.md` | 新建，PyTorch 文档镜像指南 |
